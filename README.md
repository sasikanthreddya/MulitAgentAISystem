| Command | Runs |
|---------|------|
| `npm run ui:sanity` | 5 tagged core UI cases (~19s) |
| `npm run ui:regression` | all 15 UI cases |
| `npm run api:sanity` | 2 tagged core API cases (~3s) |
| `npm run api:regression` | all 6 API cases |
| `npm run sanity` | both sanity subsets combined |
| `npm run regression` | unchanged — everything including IVR |
